# 数据库面试题（MongoDB & MySQL）

## 数据库基础

### 1. 关系型数据库 vs 非关系型数据库

**关系型数据库（RDBMS）：**
- 使用表和行的结构
- 遵循 ACID 原则
- 使用 SQL 语言
- 数据关系清晰
- 代表：MySQL、PostgreSQL、Oracle、SQL Server

**非关系型数据库（NoSQL）：**
- 不使用固定表结构
- 更适合大规模数据
- 灵活的数据模型
- 代表：MongoDB、Redis、Cassandra、Neo4j

**对比表格：**

| 特性      | 关系型数据库         | 非关系型数据库       |
| --------- | -------------------- | -------------------- |
| 数据模型  | 表结构（行列）       | 文档、键值、图等     |
| 查询语言  | SQL                  | 各种查询语言         |
| ACID 支持 | 完整支持             | 部分支持             |
| 扩展性    | 垂直扩展             | 水平扩展             |
| 事务      | 支持                 | 部分支持             |
| 适用场景  | 结构化数据、复杂查询 | 大规模数据、灵活结构 |

### 2. ACID 原则

**A（Atomicity）原子性：**
- 事务要么全部成功，要么全部失败
- 不可部分执行

**C（Consistency）一致性：**
- 事务执行前后数据库保持一致
- 不违反数据完整性约束

**I（Isolation）隔离性：**
- 并发事务之间相互隔离
- 不同隔离级别

**D（Durability）持久性：**
- 事务提交后，数据永久保存
- 即使系统故障也不会丢失

## MySQL 基础

### 1. MySQL 数据类型

**数值类型：**
```sql
TINYINT      -- 1 字节，-128 到 127
SMALLINT     -- 2 字节
MEDIUMINT    -- 3 字节
INT/INTEGER  -- 4 字节
BIGINT       -- 8 字节
FLOAT        -- 单精度浮点数
DOUBLE       -- 双精度浮点数
DECIMAL(M,D) -- 精确小数，M 总位数，D 小数位数
```

**字符串类型：**
```sql
CHAR(n)      -- 固定长度，最多 255 字符
VARCHAR(n)   -- 可变长度，最多 65535 字符
TEXT         -- 长文本，最多 65535 字符
MEDIUMTEXT   -- 中等文本，最多 16MB
LONGTEXT     -- 长文本，最多 4GB
BLOB         -- 二进制大对象
```

**日期时间类型：**
```sql
DATE         -- YYYY-MM-DD
TIME         -- HH:MM:SS
DATETIME     -- YYYY-MM-DD HH:MM:SS
TIMESTAMP    -- 时间戳，自动更新
YEAR         -- YYYY
```

**其他类型：**
```sql
JSON         -- JSON 数据
ENUM         -- 枚举类型
SET          -- 集合类型
```

### 2. MySQL 基本操作

**创建数据库：**
```sql
CREATE DATABASE testdb;
CREATE DATABASE testdb CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE testdb;
```

**创建表：**
```sql
CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) NOT NULL UNIQUE,
  email VARCHAR(100) NOT NULL,
  password VARCHAR(255) NOT NULL,
  age INT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_email (email),
  INDEX idx_username (username)
);
```

**插入数据：**
```sql
-- 单条插入
INSERT INTO users (username, email, password, age)
VALUES ('alice', 'alice@example.com', 'password123', 25);

-- 批量插入
INSERT INTO users (username, email, password, age)
VALUES
  ('bob', 'bob@example.com', 'password123', 30),
  ('charlie', 'charlie@example.com', 'password123', 28);

-- 从查询结果插入
INSERT INTO users (username, email, password)
SELECT username, email, password FROM old_users;
```

**查询数据：**
```sql
-- 基本查询
SELECT * FROM users;
SELECT id, username, email FROM users;

-- 条件查询
SELECT * FROM users WHERE age > 25;
SELECT * FROM users WHERE username = 'alice';
SELECT * FROM users WHERE email LIKE '%@example.com';
SELECT * FROM users WHERE age BETWEEN 20 AND 30;
SELECT * FROM users WHERE age IN (25, 30, 35);

-- 排序
SELECT * FROM users ORDER BY age DESC;
SELECT * FROM users ORDER BY age ASC, username ASC;

-- 限制
SELECT * FROM users LIMIT 10;
SELECT * FROM users LIMIT 10 OFFSET 20; -- 跳过 20 条，取 10 条

-- 分组
SELECT age, COUNT(*) as count FROM users GROUP BY age;
SELECT age, COUNT(*) as count FROM users GROUP BY age HAVING count > 1;

-- 连接
SELECT u.username, p.title
FROM users u
INNER JOIN posts p ON u.id = p.user_id;

SELECT u.username, p.title
FROM users u
LEFT JOIN posts p ON u.id = p.user_id;

SELECT u.username, p.title
FROM users u
RIGHT JOIN posts p ON u.id = p.user_id;

-- 子查询
SELECT * FROM users
WHERE id IN (SELECT user_id FROM posts WHERE status = 'published');

-- UNION
SELECT username FROM users
UNION
SELECT name FROM customers;
```

**更新数据：**
```sql
UPDATE users
SET age = 26, email = 'newemail@example.com'
WHERE id = 1;

-- 批量更新
UPDATE users
SET status = 'active'
WHERE created_at > '2024-01-01';
```

**删除数据：**
```sql
DELETE FROM users WHERE id = 1;

-- 清空表
TRUNCATE TABLE users;

-- 删除表
DROP TABLE users;
```

### 3. MySQL 索引

**索引类型：**
```sql
-- 主键索引
PRIMARY KEY (id)

-- 唯一索引
UNIQUE INDEX idx_email (email)

-- 普通索引
INDEX idx_username (username)

-- 复合索引
INDEX idx_name_age (name, age)

-- 全文索引
FULLTEXT INDEX idx_content (content)

-- 前缀索引
INDEX idx_title (title(10))
```

**创建索引：**
```sql
-- 创建表时
CREATE TABLE users (
  id INT PRIMARY KEY,
  email VARCHAR(100),
  INDEX idx_email (email)
);

-- 创建表后
CREATE INDEX idx_email ON users(email);
CREATE UNIQUE INDEX idx_username ON users(username);
ALTER TABLE users ADD INDEX idx_age (age);

-- 删除索引
DROP INDEX idx_email ON users;
ALTER TABLE users DROP INDEX idx_email;
```

**索引优化：**
```sql
-- 查看索引
SHOW INDEX FROM users;

-- 分析查询
EXPLAIN SELECT * FROM users WHERE email = 'test@example.com';

-- 强制使用索引
SELECT * FROM users FORCE INDEX (idx_email) WHERE email = 'test@example.com';

-- 忽略索引
SELECT * FROM users IGNORE INDEX (idx_email) WHERE email = 'test@example.com';
```

**索引使用原则：**
1. 经常用于 WHERE 条件的列
2. 经常用于 JOIN 的列
3. 经常用于 ORDER BY 的列
4. 不要为经常更新的列创建过多索引
5. 小表不需要索引
6. 区分度高的列适合索引
7. 复合索引遵循最左前缀原则

### 4. MySQL 事务

**事务基本操作：**
```sql
-- 开始事务
START TRANSACTION;
-- 或
BEGIN;

-- 提交事务
COMMIT;

-- 回滚事务
ROLLBACK;

-- 设置保存点
SAVEPOINT sp1;

-- 回滚到保存点
ROLLBACK TO sp1;

-- 释放保存点
RELEASE SAVEPOINT sp1;
```

**事务示例：**
```sql
START TRANSACTION;

UPDATE accounts SET balance = balance - 100 WHERE id = 1;
UPDATE accounts SET balance = balance + 100 WHERE id = 2;

-- 如果出错
-- ROLLBACK;

-- 如果成功
COMMIT;
```

**事务隔离级别：**
```sql
-- 查看隔离级别
SELECT @@transaction_isolation;

-- 设置隔离级别
SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;
SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;
SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;
SET SESSION TRANSACTION ISOLATION LEVEL SERIALIZABLE;
```

**隔离级别说明：**

| 隔离级别         | 脏读 | 不可重复读 | 幻读 | 说明                       |
| ---------------- | ---- | ---------- | ---- | -------------------------- |
| READ UNCOMMITTED | ✅    | ✅          | ✅    | 最低隔离，性能最好         |
| READ COMMITTED   | ❌    | ✅          | ✅    | 避免脏读                   |
| REPEATABLE READ  | ❌    | ❌          | ✅    | MySQL 默认，避免不可重复读 |
| SERIALIZABLE     | ❌    | ❌          | ❌    | 最高隔离，性能最差         |

**并发问题：**
- **脏读（Dirty Read）**：读取到未提交的数据
- **不可重复读（Non-repeatable Read）**：同一事务中多次读取结果不同
- **幻读（Phantom Read）**：同一事务中多次查询返回不同的行数

### 5. MySQL 锁

**表锁：**
```sql
-- 读锁
LOCK TABLES users READ;
UNLOCK TABLES;

-- 写锁
LOCK TABLES users WRITE;
UNLOCK TABLES;
```

**行锁：**
```sql
-- InnoDB 自动使用行锁
-- 共享锁（读锁）
SELECT * FROM users WHERE id = 1 LOCK IN SHARE MODE;

-- 排他锁（写锁）
SELECT * FROM users WHERE id = 1 FOR UPDATE;
```

**死锁处理：**
```sql
-- 查看死锁
SHOW ENGINE INNODB STATUS;

-- MySQL 会自动检测并回滚一个事务
```

### 6. MySQL 优化

**查询优化：**
```sql
-- 1. 使用 EXPLAIN 分析
EXPLAIN SELECT * FROM users WHERE email = 'test@example.com';

-- 2. 避免 SELECT *
SELECT id, username, email FROM users;

-- 3. 使用 LIMIT
SELECT * FROM users LIMIT 10;

-- 4. 使用索引
SELECT * FROM users WHERE email = 'test@example.com'; -- email 有索引

-- 5. 避免在 WHERE 中使用函数
-- ❌ 不好
SELECT * FROM users WHERE YEAR(created_at) = 2024;

-- ✅ 好
SELECT * FROM users WHERE created_at >= '2024-01-01' AND created_at < '2025-01-01';

-- 6. 使用 JOIN 代替子查询
-- ❌ 不好
SELECT * FROM users WHERE id IN (SELECT user_id FROM orders);

-- ✅ 好
SELECT u.* FROM users u INNER JOIN orders o ON u.id = o.user_id;
```

**索引优化：**
```sql
-- 1. 最左前缀原则
-- 索引：idx_name_age (name, age)
-- ✅ 可以使用索引
SELECT * FROM users WHERE name = 'alice';
SELECT * FROM users WHERE name = 'alice' AND age = 25;

-- ❌ 不能使用索引
SELECT * FROM users WHERE age = 25;

-- 2. 覆盖索引
-- 索引：idx_email (email)
-- ✅ 覆盖索引，不需要回表
SELECT email FROM users WHERE email = 'test@example.com';

-- 3. 索引选择性
-- 选择性 = 不重复值数量 / 总行数
-- 选择性越高，索引效果越好

-- 4. 避免过多索引
-- 索引会占用空间，降低写性能
```

**表结构优化：**
```sql
-- 1. 选择合适的数据类型
-- ✅ 使用 INT 而不是 BIGINT（如果不需要）
-- ✅ 使用 VARCHAR 而不是 TEXT（如果可能）

-- 2. 使用 NOT NULL
CREATE TABLE users (
  id INT NOT NULL,
  username VARCHAR(50) NOT NULL
);

-- 3. 使用合适的主键
-- 使用自增 INT 或 UUID

-- 4. 规范化
-- 避免数据冗余

-- 5. 分表
-- 按时间、按 ID 范围等分表
```

## MongoDB 基础

### 1. MongoDB 数据类型

```javascript
{
  // 基本类型
  string: "text",
  number: 123,
  number: 123.45,
  boolean: true,
  null: null,
  
  // 日期
  date: new Date(),
  
  // 数组
  array: [1, 2, 3],
  array: ["a", "b", "c"],
  
  // 对象/文档
  object: {
    name: "John",
    age: 30
  },
  
  // ObjectId
  _id: ObjectId("507f1f77bcf86cd799439011"),
  
  // 二进制
  binary: BinData(0, "base64encoded..."),
  
  // 正则表达式
  regex: /pattern/,
  
  // 代码
  code: function() { return "hello"; },
  
  // 时间戳
  timestamp: Timestamp(1000, 1),
  
  // 未定义
  undefined: undefined
}
```

### 2. MongoDB 基本操作

**连接数据库：**
```javascript
// Node.js
const { MongoClient } = require('mongodb');

const uri = 'mongodb://localhost:27017';
const client = new MongoClient(uri);

async function connect() {
  await client.connect();
  const db = client.db('testdb');
  return db;
}
```

**创建集合：**
```javascript
// MongoDB 会自动创建集合
// 或显式创建
await db.createCollection('users', {
  validator: {
    $jsonSchema: {
      bsonType: 'object',
      required: ['username', 'email'],
      properties: {
        username: {
          bsonType: 'string',
          description: 'must be a string and is required'
        },
        email: {
          bsonType: 'string',
          pattern: '^.+@.+$',
          description: 'must be a string and is required'
        }
      }
    }
  }
});
```

**插入文档：**
```javascript
// 插入单个文档
const result = await db.collection('users').insertOne({
  username: 'alice',
  email: 'alice@example.com',
  age: 25,
  created_at: new Date()
});

// 插入多个文档
const result = await db.collection('users').insertMany([
  {
    username: 'bob',
    email: 'bob@example.com',
    age: 30
  },
  {
    username: 'charlie',
    email: 'charlie@example.com',
    age: 28
  }
]);
```

**查询文档：**
```javascript
// 查询所有
const users = await db.collection('users').find({}).toArray();

// 条件查询
const user = await db.collection('users').findOne({ username: 'alice' });
const users = await db.collection('users').find({ age: { $gt: 25 } }).toArray();

// 查询操作符
// $gt: 大于
// $gte: 大于等于
// $lt: 小于
// $lte: 小于等于
// $ne: 不等于
// $in: 在数组中
// $nin: 不在数组中
// $and: 与
// $or: 或
// $not: 非
// $exists: 字段存在
// $regex: 正则表达式

// 示例
const users = await db.collection('users').find({
  age: { $gte: 25, $lte: 35 },
  email: { $regex: /@example\.com$/ }
}).toArray();

// 逻辑操作符
const users = await db.collection('users').find({
  $or: [
    { age: { $lt: 25 } },
    { age: { $gt: 35 } }
  ],
  $and: [
    { username: { $exists: true } },
    { email: { $exists: true } }
  ]
}).toArray();

// 数组查询
const users = await db.collection('users').find({
  tags: 'javascript'
}).toArray();

const users = await db.collection('users').find({
  tags: { $all: ['javascript', 'nodejs'] }
}).toArray();

const users = await db.collection('users').find({
  tags: { $size: 3 }
}).toArray();

// 嵌套文档查询
const users = await db.collection('users').find({
  'address.city': 'Beijing'
}).toArray();
```

**更新文档：**
```javascript
// 更新单个文档
await db.collection('users').updateOne(
  { username: 'alice' },
  { $set: { age: 26, email: 'newemail@example.com' } }
);

// 更新多个文档
await db.collection('users').updateMany(
  { age: { $lt: 25 } },
  { $set: { status: 'young' } }
);

// 更新操作符
// $set: 设置字段值
// $unset: 删除字段
// $inc: 增加数值
// $mul: 乘以数值
// $rename: 重命名字段
// $min: 如果新值更小则更新
// $max: 如果新值更大则更新
// $currentDate: 设置当前日期
// $push: 向数组添加元素
// $pull: 从数组删除元素
// $addToSet: 向数组添加元素（不重复）

// 示例
await db.collection('users').updateOne(
  { username: 'alice' },
  {
    $set: { email: 'newemail@example.com' },
    $inc: { age: 1 },
    $push: { tags: 'newtag' },
    $currentDate: { updated_at: true }
  }
);

// replaceOne（替换整个文档）
await db.collection('users').replaceOne(
  { username: 'alice' },
  {
    username: 'alice',
    email: 'alice@example.com',
    age: 26
  }
);
```

**删除文档：**
```javascript
// 删除单个文档
await db.collection('users').deleteOne({ username: 'alice' });

// 删除多个文档
await db.collection('users').deleteMany({ age: { $lt: 18 } });

// 删除所有文档
await db.collection('users').deleteMany({});

// 删除集合
await db.collection('users').drop();
```

**聚合查询：**
```javascript
// 基本聚合
const result = await db.collection('users').aggregate([
  { $match: { age: { $gte: 25 } } },
  { $group: { _id: '$age', count: { $sum: 1 } } },
  { $sort: { _id: 1 } }
]).toArray();

// 常用聚合阶段
// $match: 过滤文档
// $group: 分组
// $project: 投影（选择字段）
// $sort: 排序
// $limit: 限制数量
// $skip: 跳过数量
// $unwind: 展开数组
// $lookup: 关联查询（类似 JOIN）
// $facet: 多管道聚合

// 示例
const result = await db.collection('orders').aggregate([
  { $match: { status: 'completed' } },
  { $unwind: '$items' },
  {
    $group: {
      _id: '$items.product',
      totalQuantity: { $sum: '$items.quantity' },
      totalAmount: { $sum: { $multiply: ['$items.quantity', '$items.price'] } }
    }
  },
  { $sort: { totalAmount: -1 } },
  { $limit: 10 }
]).toArray();

// 关联查询
const result = await db.collection('orders').aggregate([
  {
    $lookup: {
      from: 'users',
      localField: 'user_id',
      foreignField: '_id',
      as: 'user'
    }
  },
  { $unwind: '$user' }
]).toArray();
```

### 3. MongoDB 索引

**创建索引：**
```javascript
// 单字段索引
await db.collection('users').createIndex({ email: 1 });
await db.collection('users').createIndex({ age: -1 }); // -1 表示降序

// 复合索引
await db.collection('users').createIndex({ username: 1, age: -1 });

// 文本索引
await db.collection('posts').createIndex({ title: 'text', content: 'text' });

// 地理空间索引
await db.collection('locations').createIndex({ location: '2dsphere' });

// 唯一索引
await db.collection('users').createIndex({ email: 1 }, { unique: true });

// 稀疏索引（只索引有该字段的文档）
await db.collection('users').createIndex({ phone: 1 }, { sparse: true });

// TTL 索引（自动删除过期文档）
await db.collection('sessions').createIndex(
  { created_at: 1 },
  { expireAfterSeconds: 3600 }
);

// 部分索引
await db.collection('users').createIndex(
  { email: 1 },
  { partialFilterExpression: { status: 'active' } }
);
```

**查看索引：**
```javascript
// 查看所有索引
const indexes = await db.collection('users').indexes();

// 分析查询
const explain = await db.collection('users')
  .find({ email: 'test@example.com' })
  .explain('executionStats');
```

**删除索引：**
```javascript
// 删除指定索引
await db.collection('users').dropIndex('email_1');

// 删除所有索引（除了 _id）
await db.collection('users').dropIndexes();
```

### 4. MongoDB 事务

```javascript
// 开始会话
const session = client.startSession();

try {
  // 开始事务
  session.startTransaction();

  // 操作 1
  await db.collection('accounts').updateOne(
    { _id: ObjectId('...') },
    { $inc: { balance: -100 } },
    { session }
  );

  // 操作 2
  await db.collection('accounts').updateOne(
    { _id: ObjectId('...') },
    { $inc: { balance: 100 } },
    { session }
  );

  // 提交事务
  await session.commitTransaction();
} catch (error) {
  // 回滚事务
  await session.abortTransaction();
  throw error;
} finally {
  // 结束会话
  await session.endSession();
}
```

### 5. MongoDB 优化

**查询优化：**
```javascript
// 1. 使用索引
// 确保查询字段有索引

// 2. 使用投影（只查询需要的字段）
const users = await db.collection('users')
  .find({}, { projection: { username: 1, email: 1 } })
  .toArray();

// 3. 使用 limit 和 skip
const users = await db.collection('users')
  .find({})
  .limit(10)
  .skip(20)
  .toArray();

// 4. 使用 hint 指定索引
const users = await db.collection('users')
  .find({ email: 'test@example.com' })
  .hint({ email: 1 })
  .toArray();

// 5. 避免使用 $regex（除非必要）
// 如果使用，确保有前缀索引

// 6. 使用 $exists: false 查询空字段时使用稀疏索引
```

**写入优化：**
```javascript
// 1. 批量写入
await db.collection('users').insertMany(documents, { ordered: false });

// 2. 使用 writeConcern
await db.collection('users').insertOne(document, {
  writeConcern: { w: 1, j: false }
});

// 3. 使用 bulkWrite
await db.collection('users').bulkWrite([
  { insertOne: { document: { username: 'alice' } } },
  { updateOne: { filter: { username: 'bob' }, update: { $set: { age: 30 } } } },
  { deleteOne: { filter: { username: 'charlie' } } }
]);
```

**索引优化：**
```javascript
// 1. 复合索引顺序：等值查询 -> 排序 -> 范围查询
// 好的索引：{ status: 1, created_at: -1 }
db.collection('orders').find({ status: 'active' }).sort({ created_at: -1 });

// 2. 覆盖索引（查询字段都在索引中）
await db.collection('users').createIndex({ username: 1, email: 1 });
const user = await db.collection('users')
  .find({ username: 'alice' }, { projection: { username: 1, email: 1 } })
  .toArray();

// 3. 部分索引（只索引部分文档）
await db.collection('users').createIndex(
  { email: 1 },
  { partialFilterExpression: { status: 'active' } }
);
```

## 常见面试题

### 1. MySQL 和 MongoDB 的区别？

**答案：**

| 特性     | MySQL                | MongoDB            |
| -------- | -------------------- | ------------------ |
| 数据模型 | 关系型（表）         | 文档型（集合）     |
| 查询语言 | SQL                  | JavaScript 对象    |
| 事务支持 | 完整支持             | 4.0+ 支持          |
| 扩展方式 | 垂直扩展             | 水平扩展           |
| 性能     | 适合复杂查询         | 适合高并发写入     |
| 适用场景 | 结构化数据、复杂关系 | 灵活结构、快速开发 |
| 索引     | B+ 树                | B 树               |
| JOIN     | 支持                 | 不支持（使用聚合） |

### 2. MySQL 索引有哪些类型？

**答案：**
1. **主键索引（PRIMARY KEY）**：唯一且非空
2. **唯一索引（UNIQUE）**：唯一值
3. **普通索引（INDEX）**：普通索引
4. **复合索引**：多个字段组合
5. **全文索引（FULLTEXT）**：全文搜索
6. **前缀索引**：只索引字段的前几个字符

**索引底层实现：**
- InnoDB：B+ 树
- MyISAM：B+ 树（非聚簇）
- Memory：哈希索引

### 3. 什么是最左前缀原则？

**答案：**
复合索引按照索引字段的顺序从左到右匹配。

```sql
-- 索引：idx_name_age_email (name, age, email)

-- ✅ 可以使用索引
SELECT * FROM users WHERE name = 'alice';
SELECT * FROM users WHERE name = 'alice' AND age = 25;
SELECT * FROM users WHERE name = 'alice' AND age = 25 AND email = 'alice@example.com';

-- ❌ 不能使用索引
SELECT * FROM users WHERE age = 25;
SELECT * FROM users WHERE email = 'alice@example.com';
SELECT * FROM users WHERE age = 25 AND email = 'alice@example.com';
```

### 4. MySQL 事务隔离级别有哪些？

**答案：**
1. **READ UNCOMMITTED（读未提交）**：最低隔离，可能脏读
2. **READ COMMITTED（读已提交）**：避免脏读，可能不可重复读
3. **REPEATABLE READ（可重复读）**：MySQL 默认，避免不可重复读，可能幻读
4. **SERIALIZABLE（串行化）**：最高隔离，避免所有问题，性能最差

### 5. 什么是脏读、不可重复读、幻读？

**答案：**
- **脏读（Dirty Read）**：读取到未提交的数据
- **不可重复读（Non-repeatable Read）**：同一事务中多次读取结果不同
- **幻读（Phantom Read）**：同一事务中多次查询返回不同的行数

### 6. MySQL 如何优化查询性能？

**答案：**
1. **使用索引**：为常用查询字段创建索引
2. **避免 SELECT ***：只查询需要的字段
3. **使用 LIMIT**：限制返回结果数量
4. **避免在 WHERE 中使用函数**：会导致索引失效
5. **使用 JOIN 代替子查询**：JOIN 通常更高效
6. **使用 EXPLAIN 分析**：查看查询执行计划
7. **优化表结构**：选择合适的数据类型
8. **使用连接池**：减少连接开销
9. **读写分离**：主从复制
10. **分库分表**：水平拆分

### 7. MongoDB 如何实现 JOIN？

**答案：**
MongoDB 不支持传统 JOIN，但可以使用：

**1. $lookup（聚合管道）：**
```javascript
const result = await db.collection('orders').aggregate([
  {
    $lookup: {
      from: 'users',
      localField: 'user_id',
      foreignField: '_id',
      as: 'user'
    }
  }
]).toArray();
```

**2. 应用层 JOIN：**
```javascript
const orders = await db.collection('orders').find({}).toArray();
const userIds = orders.map(o => o.user_id);
const users = await db.collection('users').find({ _id: { $in: userIds } }).toArray();
```

**3. 数据冗余：**
```javascript
// 在订单中存储用户信息
{
  order_id: 1,
  user: {
    name: 'Alice',
    email: 'alice@example.com'
  }
}
```

### 8. MongoDB 索引有哪些类型？

**答案：**
1. **单字段索引**：单个字段
2. **复合索引**：多个字段
3. **文本索引**：全文搜索
4. **地理空间索引**：地理位置查询
5. **唯一索引**：唯一值
6. **稀疏索引**：只索引有该字段的文档
7. **TTL 索引**：自动删除过期文档
8. **部分索引**：只索引满足条件的文档
9. **哈希索引**：等值查询

### 9. 如何选择 MySQL 和 MongoDB？

**答案：**
**选择 MySQL：**
- 数据结构固定
- 需要复杂查询和 JOIN
- 需要强一致性
- 需要事务支持
- 数据关系复杂

**选择 MongoDB：**
- 数据结构灵活
- 需要快速开发
- 高并发写入
- 水平扩展需求
- 文档型数据

**混合使用：**
- 核心数据用 MySQL
- 日志、缓存用 MongoDB
- 根据场景选择

### 10. 什么是数据库分库分表？

**答案：**
**分库：**
- 按业务拆分：用户库、订单库、商品库
- 按地域拆分：北京库、上海库

**分表：**
- **垂直分表**：按字段拆分
- **水平分表**：
  - 按范围：按时间、按 ID 范围
  - 按哈希：按 ID 取模
  - 按一致性哈希

**分库分表策略：**
```sql
-- 按 ID 取模
-- 表：users_0, users_1, users_2
SELECT * FROM users_{user_id % 3} WHERE id = ?;

-- 按时间分表
-- 表：orders_2024_01, orders_2024_02
SELECT * FROM orders_{DATE_FORMAT(date, '%Y_%m')} WHERE ...;
```

### 11. MySQL 主从复制原理是什么？

**答案：**
1. **主库（Master）**：记录所有变更到二进制日志（binlog）
2. **从库（Slave）**：连接主库，读取 binlog
3. **IO 线程**：从库 IO 线程读取主库 binlog
4. **SQL 线程**：从库 SQL 线程执行 binlog 中的 SQL

**复制方式：**
- **异步复制**：主库不等待从库确认
- **半同步复制**：至少一个从库确认
- **全同步复制**：所有从库确认

### 12. MongoDB 的副本集是什么？

**答案：**
MongoDB 副本集是一组维护相同数据集的 MongoDB 实例。

**角色：**
- **Primary（主节点）**：处理所有写操作
- **Secondary（从节点）**：复制主节点数据
- **Arbiter（仲裁节点）**：投票选举，不存储数据

**工作原理：**
1. 客户端写入主节点
2. 主节点记录操作日志（oplog）
3. 从节点复制 oplog 并应用
4. 主节点故障时，从节点选举新的主节点

### 13. 如何优化 MongoDB 查询性能？

**答案：**
1. **创建索引**：为查询字段创建索引
2. **使用投影**：只查询需要的字段
3. **使用 limit**：限制返回数量
4. **使用 hint**：指定使用哪个索引
5. **避免正则**：$regex 无法有效使用索引
6. **使用覆盖索引**：查询字段都在索引中
7. **优化聚合管道**：$match 尽量靠前
8. **使用 explain**：分析查询计划
9. **批量操作**：使用 insertMany、bulkWrite
10. **连接池**：复用连接

### 14. MySQL InnoDB 和 MyISAM 的区别？

**答案：**

| 特性     | InnoDB            | MyISAM                     |
| -------- | ----------------- | -------------------------- |
| 事务     | 支持              | 不支持                     |
| 外键     | 支持              | 不支持                     |
| 锁粒度   | 行锁              | 表锁                       |
| 崩溃恢复 | 支持              | 不支持                     |
| 全文索引 | 5.6+ 支持         | 支持                       |
| 存储文件 | .ibd（数据+索引） | .MYD（数据）+ .MYI（索引） |
| 适用场景 | 事务、高并发      | 读多写少、简单查询         |

### 15. 什么是数据库连接池？

**答案：**
数据库连接池是预先创建并维护的数据库连接集合，用于复用连接，提高性能。

**优势：**
- 减少连接创建和销毁开销
- 控制并发连接数
- 统一管理连接
- 提高应用性能

**实现：**
```javascript
// Node.js 使用 mysql2
const pool = mysql.createPool({
  connectionLimit: 10,
  host: 'localhost',
  user: 'root',
  password: 'password',
  database: 'testdb'
});

// 使用
const [rows] = await pool.query('SELECT * FROM users');
```

## 最佳实践

### 1. MySQL 最佳实践

**设计：**
- ✅ 规范化设计
- ✅ 选择合适的数据类型
- ✅ 使用 NOT NULL
- ✅ 合理使用索引
- ✅ 避免过度设计

**查询：**
- ✅ 使用 EXPLAIN 分析
- ✅ 避免 SELECT *
- ✅ 使用 LIMIT
- ✅ 使用索引
- ✅ 避免在 WHERE 中使用函数

**性能：**
- ✅ 使用连接池
- ✅ 读写分离
- ✅ 分库分表
- ✅ 定期优化表
- ✅ 监控慢查询

### 2. MongoDB 最佳实践

**设计：**
- ✅ 合理嵌入 vs 引用
- ✅ 避免过度嵌套
- ✅ 使用合适的数据类型
- ✅ 合理使用索引

**查询：**
- ✅ 使用投影
- ✅ 使用索引
- ✅ 使用 limit
- ✅ 分析查询计划

**性能：**
- ✅ 使用连接池
- ✅ 批量操作
- ✅ 副本集
- ✅ 分片集群
- ✅ 监控性能

### 3. 数据安全

**MySQL：**
- ✅ 使用参数化查询（防 SQL 注入）
- ✅ 最小权限原则
- ✅ 加密敏感数据
- ✅ 定期备份
- ✅ 审计日志

**MongoDB：**
- ✅ 使用参数化查询
- ✅ 启用认证
- ✅ 加密传输
- ✅ 定期备份
- ✅ 访问控制

## 总结

数据库是现代应用的核心，掌握 MySQL 和 MongoDB 需要：

**MySQL：**
1. SQL 语言基础
2. 索引优化
3. 事务和锁
4. 查询优化
5. 主从复制

**MongoDB：**
1. 文档操作
2. 索引创建
3. 聚合查询
4. 事务处理
5. 副本集和分片

**通用技能：**
- 性能优化
- 安全防护
- 备份恢复
- 监控运维

持续学习，在实践中提升数据库能力！
